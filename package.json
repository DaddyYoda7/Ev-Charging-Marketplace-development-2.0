{
  "name": "evconnect-ai",
  "version": "2.0.0",
  "description": "EV Connect AI - Pan-India EV Infrastructure, Real-Time Telemetry, OCPP 2.0 & Booking Marketplace",
  "main": "server/index.js",
  "scripts": {
    "dev": "node start.js",
    "dev:server": "node server/index.js",
    "dev:client": "npm run dev --prefix client",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "node server/index.js",
    "seed": "node server/seed.js"
  },
  "keywords": [
    "ev",
    "charging",
    "marketplace",
    "telemetry",
    "ocpp",
    "india"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "sqlite3": "^5.1.7"
  }
}
